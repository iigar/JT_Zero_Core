<analysis>**original_problem_statement**: 
The user is building a complex robotics runtime, JT-Zero, for a drone on a Raspberry Pi (supporting Zero 2 W and Pi 4). The system includes a high-performance C++ core for Visual Odometry (VO), a FastAPI backend, and a React monitoring dashboard. Recent requests focused on fixing the USB thermal camera to pull real hardware feeds instead of mocked data, and making the thermal camera stream continuously in the frontend dashboard. 

**User's preferred language**: **Ukrainian**. (INSTRUCT: The next agent MUST respond to the user in Ukrainian only).

**what currently exists?** 
A full-stack hardware-in-the-loop robotics application. The C++ core handles visual odometry via a primary CSI camera (which is currently working and displaying correctly on the physical Pi's dashboard). The React frontend is auto-built and pre-packaged in  to avoid OOM issues on the Pi. The secondary USB thermal camera uses an AV-to-USB capture card. The system relies on a  wrapper around  to capture frames because standard Python  fails on aarch64. 

**Last working item**: 
    - Last item agent was working: Fixing the USB thermal camera live stream on the physical Raspberry Pi. The capture card requires warm-up frames (first few are black) and only outputs valid data in MJPEG format (YUYV returns all zeros). The agent was trying to find a reliable way to capture MJPEG frames continuously without the stream freezing.
    - Status: USER VERIFICATION PENDING
    - Agent Testing Done: Y
    - Which testing method agent to use? Manual testing via bash/python scripts (Note: Final testing requires physical hardware testing by the user).
    - User Testing Done: N (Agent just pushed a revert to a batch-capture method  and is waiting for the user to test).

**All Pending/In progress Issue list**: 
  Issue 1: 🔴 USB Thermal Camera frame freezing / low FPS (P0)
     - Attempted fixes: Agent tried a continuous non-blocking pipe read ( with ), but it caused the stream to freeze after 1 frame or hang completely on the Pi. Single frame capture () resulted in black screens due to lack of device warm-up. Agent currently reverted to a batch capture ( taking the last frame) which is slow (low FPS) but theoretically avoids the freeze.
     - Next debug checklist: Check . Wait for the user to confirm if the batch-capture method produces video. If the user requests higher FPS, debug why the continuous  +  approach freezes on the physical Pi (possibly buffer filling, or failure to cleanly parse MJPEG stream boundaries).
     - Why fix this issue and what will be achieved with the fix? Provides real-time thermal situational awareness for the drone at a usable frame rate.
     - Status: USER VERIFICATION PENDING
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? Both (Requires physical hardware testing).
     - Blocked on other issue: None.

**In progress Task List**: 
  Task 1: 🟡 VO Fallback to USB Thermal Camera (P1)
     - Where to resume: Implement logic so that if the primary CSI camera loses tracking (e.g., CONF < 10% or complete darkness), the system automatically uses the secondary USB thermal camera for Visual Odometry.
     - What will be achieved with this? Allows the drone to maintain position holding and navigation using thermal signatures in completely dark or featureless visual environments.
     - Status: NOT STARTED
     - Should Test frontend/backend/both after fix? Backend / C++ Core.
     - Blocked on something: Blocked until the USB Thermal camera streaming (Issue 1) is 100% reliable.

**Upcoming and Future Tasks**
    Upcoming Tasks:
    - (P1) Implement VO Fallback to USB camera when CSI loses tracking.
    Future Tasks:
    - (P2) IP camera (RTSP) support.
    - (P2) ARM NEON optimization for the C++ core.
    - (P2) Autonomous Mission Planning UI/features.
    - (P2) Calibration of focal length for USB thermal cameras.
    - (P3) MAVLink diagnostics page addition in the React Dashboard.

**Completed work in this session**
- Identified and fixed WebSocket connection bug: Removed hardcoded Emergent URL () from the React build so the dashboard connects to the local Pi instead of the Emergent simulator (DONE).
- Updated  for auto-streaming and dynamic formatting (grayscale vs jpeg) bypassing the iron palette for native colored streams (DONE).
- Changed V4L2 capture format to MJPEG in  because the AV-to-USB hardware outputs all zeros for YUYV (DONE).
- Updated , , and  to support variable frame formats and pass MJPEG directly to the frontend (DONE).

**Earlier issues found/mentioned but not fixed**
   - None.

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: V4L2 capture card compatibility (first architecture mismatches, now warm-up/buffer freezing issues).
  - Recurrence count: 2
  - Status: IN PROGRESS

**Code Architecture**


**Key Technical Concepts**
- **Full-Stack:** FastAPI + React + C++ Core.
- **Pre-built Frontend Architecture:** The Pi Zero 2W lacks RAM to compile React. The frontend is built locally in the Emergent environment and committed directly to  so FastAPI can serve the static files. 
- **MJPEG over V4L2 Subprocess:** Due to 64-bit architecture issues, standard  fails. Video is fetched via  subprocess.

**key DB schema**
  - N/A (Hardware runtime, no database).

**changes in tech stack**
  - None.

**All files of reference** 
-  (Heavily modified to test batch capturing and MJPEG stream parsing).
-  (Updated to handle raw JPEG images without applying false-color palettes).
-  (Where WebSocket connects).
- , ,  (Updated to handle  metadata).

**Areas that need refactoring**: 
- : The  logic for reading the V4L2 pipe is brittle. If the user requires high FPS, this may need to be refactored into a proper C++ V4L2/OpenCV integration, as Python non-blocking reads of  output keep freezing the stream on the physical hardware.

**key api endpoints**
-  (Returns active camera slots, resolutions, exact sensor names, and ).
-  (Endpoint for continuous thermal streaming, now returning ).

**Critical Info for New Agent** 
- **CRITICAL FRONTEND BUILD RULE:** When building the frontend locally in Emergent to push to , you MUST ensure  is empty. If you build it with the Emergent Preview URL, the user's dashboard on their physical Pi will connect its WebSockets to the Emergent simulator instead of the Pi's real sensors.
- **Hardware constraints:** The user runs this on physical Raspberry Pi hardware. Local testing on Emergent environment will only yield simulated results.
- **Camera Hardware Quirks:** The user's AV-to-USB capture card *only* works in MJPEG mode (YUYV yields a blank stream of 0s). It also requires device warm-up frames, meaning the first 3-5 frames grabbed upon opening the device are completely black.
- **GitHub Actions Note:** The CI/CD for the frontend is currently failing due to a path issue, which is why the agent was manually building the frontend (yarn run v1.22.22
info Visit https://yarnpkg.com/en/docs/cli/run for documentation about this command.) and moving it to  via bash commands before committing.

**documents and test reports created in this job** 
- None.

**Last 10 User Messages and any pending HUMAN messages** 
1. User ran update script, cameras showed as ACTIVE but video UI was wrong.
2. User provided screenshot showing the old UI was still rendering.
3. Agent manually built the frontend and pushed it.
4. User pulled, but screenshot showed Primary camera said SIMULATED instead of CSI.
5. Agent realized the React build had hardcoded the Emergent Preview URL, causing the Pi to connect to the Emergent Simulator. Agent rebuilt with an empty URL.
6. User reported CSI camera now works! But Thermal camera is completely black.
7. Agent applied auto-contrast to React UI thinking it was a palette issue. Didn't work.
8. User ran raw  bash tests at agent's request. Revealed YUYV is broken on this device, but MJPEG works.
9. Agent updated pipeline to use MJPEG. Image appeared but immediately froze.
10. User asked why FPS couldn't be increased. Agent tried non-blocking continuous pipe reading, which resulted in complete hangs. 
*Pending:* Agent just reverted to  batch reads to bypass the hanging issue and guarantee at least 1 reliable frame, and is waiting for user to verify if this restored the video feed.

**Project Health Check:**
- Broken: High FPS continuous thermal streaming on physical Pi.
- Mocked: Sensors/Cameras when running inside Emergent container.

**3rd Party Integrations** 
- GitHub Actions (CI/CD)

**Testing status**
  - Testing agent used after significant changes: NO (requires physical hardware).
  - Troubleshoot agent used after agent stuck in loop: NO.
  - Test files created: None.
  - Known regressions: Continuous high-FPS thermal streaming is currently struggling due to pipe freezing.

**Credentials to test flow:**
- N/A

**What agent forgot to execute**
- The agent was completely side-tracked by fixing the WebSocket URL bug and fighting Linux V4L2 pipe freezes. The highly requested feature VO fallback to USB (Task 1) was not started.</analysis>
